package com.unexceptional.beast.banko.newVersion.callback;

import com.unexceptional.beast.banko.newVersion.db.model.Budget;

public interface BudgetClickCallback {
    void onBudgetItemClick(Budget budget);
}
